// let button = document.querySelector("button");

// button.addEventListener("click", () => {
//   let images = document.querySelectorAll("img");
//   let randomNumbers = [];
//   let randomImageSource = [];

//   for (let i = 0; i < 2; i++) {
//     randomNumbers[i] = Math.floor(Math.random() * 6 + 1);
    
//     randomImageSource[i] = `Images/${randomNumbers[i]}.png`;
//     // console.log(randomImageSource);
//     images[i].setAttribute("src", randomImageSource[i]);
 
//   }

//   let resultText;
//   if (randomNumbers[0] < randomNumbers[1]) {
//     resultText = "Player 1 wins!";
//   } else if (randomNumbers[0] > randomNumbers[1]) {
//     resultText = "Player 2 wins!";
//   } else {
//     resultText = "It's a tie!";
//   }

//   document.querySelector("h1").textContent = resultText;
// });


let button = document.querySelector("button");
button.addEventListener('click',()=>{
    let images = document.querySelectorAll('img');
    
})